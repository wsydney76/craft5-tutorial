{# @param query EntryQuery #}

{% set entries = query.collect %}

{% if entries.count %}
    {# Output content builder entries with Tailwind typography. #}
    <div class="my-8 prose prose-lg prose-slate max-w-none hyphens-auto">
        {{ entries.render({entries}) }}
    </div>
{% endif %}